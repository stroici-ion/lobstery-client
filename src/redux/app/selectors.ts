import { RootState } from '..';

// export const selectPostToEdit = (state: RootState) => state.app.editPost;
// export const selectPostToEditCandidate = (state: RootState) => state.app.editPostCandidate;
// export const selectPostToCreate = (state: RootState) => state.app.post;
// export const selectIsEditingPost = (state: RootState) => state.app.isEditing;

// export const selectNewPost = (state: RootState) =>
//   state.app[`${state.app.isEditing ? 'post' : 'editPost'}`];
// export const selectNewPostImages = (state: RootState) =>
//   state.app[`${state.app.isEditing ? 'post' : 'editPost'}`].images;
// export const selectTaggedFriends = (state: RootState) =>
//   state.app[`${state.app.isEditing ? 'post' : 'editPost'}`].tagged_friends;

// export const selectExistsNewPostDraft = (state: RootState) =>
//   state.app[`${state.app.isEditing ? 'post' : 'editPost'}`].title ||
//   state.app[`${state.app.isEditing ? 'post' : 'editPost'}`].feeling ||
//   state.app[`${state.app.isEditing ? 'post' : 'editPost'}`].text ||
//   state.app[`${state.app.isEditing ? 'post' : 'editPost'}`].images?.length ||
//   state.app[`${state.app.isEditing ? 'post' : 'editPost'}`].tagged_friends?.length ||
//   state.app[`${state.app.isEditing ? 'post' : 'editPost'}`].tags?.length;
